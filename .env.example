# URL воркера админки (см. worker/README.md). Пусто = адрес по умолчанию не задан,
# запросы /admin будут падать, пока воркер не задеплоен и переменная не указана.
REACT_APP_ADMIN_API_URL=https://komnata1908-admin.<account>.workers.dev
